# DocuVault — Release Digest

_Plain-English summary of what each merge changed, generated by Code2Docs._

## v1.5 — 3 Jul 2026  (PR #22)

**1 flow(s) visibly changed** — 1 new step(s), 0 changed; 8 more re-checked.

[▶ Open the interactive living docs](interactive/index.html)

### Registering user
- 🆕 New step — the assistant now also records welcome sent <sub>(recordWelcomeSent)</sub>.
- _Touches:_ User, Document, VaultLogger, CipherUtil, AuditEvent.

**Also re-checked (no visible flow change):** Opening document, Revoking access, Sharing document, Uploading document, Deleting document, Generating report, Rename document, Retention sweep.

**Files changed in this module:** `main/java/com/docuvault/model/User.java`, `main/java/com/docuvault/repository/UserRepository.java`, `main/java/com/docuvault/service/UserService.java`

<sub>Generated by Code2Docs from the code graph — review before publishing.</sub>

---


## v1.4 — 3 Jul 2026  (PR #20)

**1 flow(s) visibly changed** — 1 new step(s), 0 changed; 8 more re-checked.

[▶ Open the interactive living docs](interactive/index.html)

### Uploading document
- 🆕 New step — the assistant now also records protection <sub>(recordProtection)</sub>.
- _Touches:_ Document, VaultLogger, CipherUtil, EncryptionKey, Document.

**Also re-checked (no visible flow change):** Opening document, Revoking access, Sharing document, Registering user, Deleting document, Generating report, Rename document, Retention sweep.

**Files changed in this module:** `main/java/com/docuvault/model/Document.java`, `main/java/com/docuvault/repository/DocumentRepository.java`, `main/java/com/docuvault/service/DocumentService.java`

<sub>Generated by Code2Docs from the code graph — review before publishing.</sub>

---


## v1.3 — 3 Jul 2026  (PR #18)

**5 flow(s) visibly changed** — 19 new step(s), 0 changed; 3 more re-checked.

[▶ Open the interactive living docs](interactive/index.html)

### Opening document
- 🆕 New step — the assistant now also touch last accessed <sub>(touchLastAccessed)</sub>.
- _Touches:_ Document, VaultLogger, Policy, Document.

### Uploading document
- 🆕 New step — the assistant now also scans upload <sub>(scanUpload)</sub>.
- _Touches:_ Document, VaultLogger, CipherUtil, EncryptionKey, Document.

### Deleting document
- 🆕 New step — the assistant now also records shred <sub>(recordShred)</sub>.
- _Touches:_ Document, EncryptionKey, VaultLogger, Document.

### Rename document
- 🆕 New step — the assistant now also looks up file name <sub>(getFileName)</sub>.
- 🆕 New step — the assistant now also writes an info log line <sub>(logInfo)</sub>.
- 🆕 New step — the assistant now also emits <sub>(emit)</sub>.
- 🆕 New step — the assistant now also rename document <sub>(renameDocument)</sub>.
- 🆕 New step — the assistant now also checks access <sub>(checkAccess)</sub>.
- 🆕 New step — the assistant now also evaluates policy <sub>(evaluatePolicy)</sub>.
- 🆕 New step — the assistant now also looks up policy for user <sub>(findPolicyForUser)</sub>.
- 🆕 New step — the assistant now also looks up permission <sub>(getPermission)</sub>.
- _Touches:_ .logInfo, Document, Policy.

### Retention sweep
- 🆕 New step — the assistant now also writes an info log line <sub>(logInfo)</sub>.
- 🆕 New step — the assistant now also emits <sub>(emit)</sub>.
- 🆕 New step — the assistant now also sweep expired documents <sub>(sweepExpiredDocuments)</sub>.
- 🆕 New step — the assistant now also looks up all documents <sub>(findAllDocuments)</sub>.
- 🆕 New step — the assistant now also looks up uploaded at <sub>(getUploadedAt)</sub>.
- 🆕 New step — the assistant now also shred expired <sub>(shredExpired)</sub>.
- 🆕 New step — the assistant now also destroys key <sub>(destroyKey)</sub>.
- 🆕 New step — the assistant now also deletes key <sub>(deleteKey)</sub>.
- _Touches:_ .logInfo, Document, EncryptionKey.

**Also re-checked (no visible flow change):** Revoking access, Sharing document, Registering user.

**Files changed in this module:** `main/java/com/docuvault/model/Document.java`, `main/java/com/docuvault/repository/DocumentRepository.java`, `main/java/com/docuvault/service/DocumentService.java`

<sub>Generated by Code2Docs from the code graph — review before publishing.</sub>

---


## v1.3 — 3 Jul 2026  (PR #16)

**4 flow(s) visibly changed** — 18 new step(s), 0 changed; 5 more re-checked.

[▶ Open the interactive living docs](interactive/index.html)

### Opening document
- 🆕 New step — the assistant now also touch last accessed <sub>(touchLastAccessed)</sub>.
- _Touches:_ Document, VaultLogger, Policy, Document.

### Uploading document
- 🆕 New step — the assistant now also scans upload <sub>(scanUpload)</sub>.
- _Touches:_ Document, VaultLogger, CipherUtil, EncryptionKey, Document.

### Rename document
- 🆕 New step — the assistant now also looks up file name <sub>(getFileName)</sub>.
- 🆕 New step — the assistant now also writes an info log line <sub>(logInfo)</sub>.
- 🆕 New step — the assistant now also emits <sub>(emit)</sub>.
- 🆕 New step — the assistant now also rename document <sub>(renameDocument)</sub>.
- 🆕 New step — the assistant now also checks access <sub>(checkAccess)</sub>.
- 🆕 New step — the assistant now also evaluates policy <sub>(evaluatePolicy)</sub>.
- 🆕 New step — the assistant now also looks up policy for user <sub>(findPolicyForUser)</sub>.
- 🆕 New step — the assistant now also looks up permission <sub>(getPermission)</sub>.
- _Touches:_ .logInfo, Document, Policy.

### Retention sweep
- 🆕 New step — the assistant now also writes an info log line <sub>(logInfo)</sub>.
- 🆕 New step — the assistant now also emits <sub>(emit)</sub>.
- 🆕 New step — the assistant now also sweep expired documents <sub>(sweepExpiredDocuments)</sub>.
- 🆕 New step — the assistant now also looks up all documents <sub>(findAllDocuments)</sub>.
- 🆕 New step — the assistant now also looks up uploaded at <sub>(getUploadedAt)</sub>.
- 🆕 New step — the assistant now also shred expired <sub>(shredExpired)</sub>.
- 🆕 New step — the assistant now also destroys key <sub>(destroyKey)</sub>.
- 🆕 New step — the assistant now also deletes key <sub>(deleteKey)</sub>.
- _Touches:_ .logInfo, Document, EncryptionKey.

**Also re-checked (no visible flow change):** Revoking access, Sharing document, Registering user, Deleting document, Generating report.

**Files changed in this module:** `main/java/com/docuvault/model/Document.java`, `main/java/com/docuvault/repository/DocumentRepository.java`, `main/java/com/docuvault/service/DocumentService.java`

<sub>Generated by Code2Docs from the code graph — review before publishing.</sub>

---


## v1.2 — 3 Jul 2026  (initial documentation)

**Code2Docs is now live for DocuVault.** 7 flow(s) documented as interactive living docs.

[▶ Open the interactive living docs](interactive/index.html)

- **Opening document** — 
- **Revoking access** — 
- **Sharing document** — 
- **Uploading document** — 
- **Registering user** — 
- **Deleting document** — 
- **Generating report** — 

<sub>Generated by Code2Docs from the code graph. Future merges will append what each one changed.</sub>

---
